---
import BaseLayout from "../layouts/BaseLayout.astro";
import SocialLinks from "../components/SocialLinks.astro";

const success = Astro.url.searchParams.get('success') === 'true';
---

<BaseLayout title="Contacto" description="Contacta con Legacy of the Seas para booking, prensa o cualquier consulta. Estamos aquí para ayudarte.">
  <!-- Hero section -->
  <section class="py-20 bg-band-dark-50">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-4xl md:text-6xl font-metal text-gradient mb-6">
        Contacto
      </h1>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto">
        ¿Tienes alguna pregunta? ¿Quieres contratarnos para un evento?
        Estamos aquí para escucharte.
      </p>
    </div>
  </section>

  <!-- Contact content -->
  <section class="py-16 bg-band-dark">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">

        <!-- Contact form -->
        <div>
          <h2 class="text-2xl font-metal text-band-sea mb-6">
            Envíanos un mensaje
          </h2>

          {success && (
            <div class="mb-6 p-4 bg-green-900/50 border border-green-500/50 rounded-lg">
              <p class="text-green-400 font-medium">
                ¡Mensaje enviado correctamente! Te responderemos pronto.
              </p>
            </div>
          )}

          <form
            action="https://api.web3forms.com/submit"
            method="POST"
            class="space-y-6"
          >
            <input type="hidden" name="access_key" value="dc1cfcbf-751c-429b-80ec-0dba80560eea" />
            <input type="hidden" name="subject" value="Nuevo mensaje desde legacyoftheseas.pages.dev" />
            <input type="hidden" name="from_name" value="Legacy of the Seas Web" />
            <input type="hidden" name="redirect" value="https://legacyoftheseas.pages.dev/contacto?success=true" />
            <input type="checkbox" name="botcheck" class="hidden" style="display: none;" />

            <!-- Name -->
            <div>
              <label for="name" class="block text-sm font-medium text-gray-300 mb-2">
                Nombre *
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 bg-band-dark-100 border border-band-sea/20 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-band-sea focus:ring-1 focus:ring-band-sea transition-colors"
                placeholder="Tu nombre"
              />
            </div>

            <!-- Email -->
            <div>
              <label for="email" class="block text-sm font-medium text-gray-300 mb-2">
                Email *
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 bg-band-dark-100 border border-band-sea/20 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-band-sea focus:ring-1 focus:ring-band-sea transition-colors"
                placeholder="tu@email.com"
              />
            </div>

            <!-- Asunto (para Web3Forms y UX) -->
            <div>
              <label for="asunto" class="block text-sm font-medium text-gray-300 mb-2">
                Asunto *
              </label>
              <select
                id="asunto"
                name="asunto"
                required
                class="w-full px-4 py-3 bg-band-dark-100 border border-band-sea/20 rounded-lg text-white focus:outline-none focus:border-band-sea focus:ring-1 focus:ring-band-sea transition-colors"
              >
                <option value="">Selecciona un asunto</option>
                <option value="Booking / Contratación">Booking / Contratación</option>
                <option value="Prensa / Entrevistas">Prensa / Entrevistas</option>
                <option value="Colaboración">Colaboración</option>
                <option value="Merchandising">Merchandising</option>
                <option value="Otro">Otro</option>
              </select>
            </div>

            <!-- Message -->
            <div>
              <label for="message" class="block text-sm font-medium text-gray-300 mb-2">
                Mensaje *
              </label>
              <textarea
                id="message"
                name="message"
                rows="5"
                required
                class="w-full px-4 py-3 bg-band-dark-100 border border-band-sea/20 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-band-sea focus:ring-1 focus:ring-band-sea transition-colors resize-none"
                placeholder="Escribe tu mensaje aquí..."
              ></textarea>
            </div>

            <!-- Submit -->
            <button type="submit" class="btn-primary w-full sm:w-auto">
              Enviar mensaje
            </button>
          </form>
        </div>

        <!-- Contact info -->
        <div class="lg:pl-8">
          <h2 class="text-2xl font-metal text-band-sea mb-6">
            Información de contacto
          </h2>

          <!-- Email contacto -->
          <div class="mb-8">
            <h3 class="text-sm uppercase tracking-wider text-gray-400 mb-2">
              Email
            </h3>
            <a
              href="mailto:legacy.of.the.seas@gmail.com"
              class="text-lg text-white hover:text-band-sea transition-colors"
            >
              legacy.of.the.seas@gmail.com
            </a>
          </div>

          <!-- Social links -->
          <div class="mb-8">
            <h3 class="text-sm uppercase tracking-wider text-gray-400 mb-4">
              Síguenos
            </h3>
            <SocialLinks size="lg" />
          </div>

          <!-- Response time note -->
          <div class="bg-band-dark-100 border border-band-sea/20 rounded-lg p-6">
            <p class="text-gray-400 text-sm">
              <strong class="text-band-sea">Nota:</strong> Intentamos responder a todos los mensajes
              en un plazo de 48-72 horas. Para consultas urgentes de booking,
              por favor indicarlo en el asunto.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
