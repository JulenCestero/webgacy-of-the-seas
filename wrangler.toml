name = "legacy-of-the-seas"
compatibility_date = "2024-01-01"

# R2 bucket para uploads de imágenes
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "legacy-uploads"

# Variables de entorno (se configuran en Cloudflare Dashboard)
[vars]
# TURSO_DATABASE_URL y TURSO_AUTH_TOKEN se configuran como secrets
